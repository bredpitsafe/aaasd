import { green, red } from '@frontend/common/src/utils/colors';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnTrue = style({
    color: green[5],
});
export const cnFalse = style({
    color: red[5],
});
