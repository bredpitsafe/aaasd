import { specify } from '@frontend/common/src/utils/css/specify';
import { style } from '@frontend/common/src/utils/css/style.css';
export const cnFullHeightTab = style({
    padding: '4px',
    height: '100%',
});

export const cnTableTitle = style(
    specify({
        margin: '4px 0 2px',
    }),
);
