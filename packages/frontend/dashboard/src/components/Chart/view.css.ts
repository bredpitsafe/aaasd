import { style } from '@frontend/common/src/utils/css/style.css';

export const cnRoot = style({
    overflow: 'hidden',
    display: 'flex',
    flexDirection: 'column',
    height: '100%',
    gap: '4px',
});
export const cnCanvas = style({
    flexGrow: 1,
});
