import { blue, green, red } from '@frontend/common/src/utils/colors';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnIcon = style({
    marginRight: '10px',
});
export const cnRedIcon = style({
    color: red[5],
});
export const cnGreenIcon = style({
    color: green[5],
});
export const cnBlueIcon = style({
    color: blue[5],
});
