import { style } from '@frontend/common/src/utils/css/style.css';

export const cnModalHeader = style({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
});

export const cnAddAndChooseDashboard = style({
    display: 'flex',
    alignItems: 'center',
    gap: '4px',
});
