import { green, red } from '@frontend/common/src/utils/colors';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnType = style({
    fontWeight: 'bold',
    fontSize: 12,
});

export const cnMaker = style({
    color: green[5],
});

export const cnTaker = style({
    color: red[5],
});
