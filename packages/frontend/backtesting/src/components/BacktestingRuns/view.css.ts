import { cnCol } from '@frontend/common/src/utils/css/common.css';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnRoot = cnCol;

export const cnTable = style({
    flexGrow: 1,
});
