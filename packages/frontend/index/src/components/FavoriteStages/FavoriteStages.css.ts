import { styleTooltipContainer } from '@frontend/common/src/components/Tooltip.css.ts';
import { style } from '@frontend/common/src/utils/css/style.css.ts';

export const cnTooltip = style({});

styleTooltipContainer(cnTooltip, { padding: 0 });
