import { style } from '@frontend/common/src/utils/css/style.css';

export const cnLinkArray = style({
    marginRight: '4px',
    selectors: {
        '&:last-child': {
            marginLeft: 0,
        },
    },
});
