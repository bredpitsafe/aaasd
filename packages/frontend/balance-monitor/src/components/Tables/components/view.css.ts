import { style } from '@frontend/common/src/utils/css/style.css';

export const cnDisableMouse = style({
    pointerEvents: 'none',
});

export const cnGridCellBlock = style({
    flexWrap: 'nowrap',
});
