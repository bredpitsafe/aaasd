import { style } from '@frontend/common/src/utils/css/style.css';

export const cnComponentStatuses = style({
    marginTop: '12px',
    marginBottom: '12px',
});

export const cnNewManualTransfer = style({
    marginTop: '12px',
});
