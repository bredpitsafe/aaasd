export enum ERunsTabSelectors {
    RunsTab = '[class*="RunsTab"]',
    SaveScoreIndicatorButton = '[class*="RunsTab"] [data-test="saveScoreIndicatorButton"]',
    ScoreIndicatorSearchInput = '[class*="RunsTab"] [data-test="scoreIndicatorInput"]',
    DeleteScoreIndicatorButton = '[class*="RunsTab"] [data-test="scoreIndicatorInput"] [class*="remove"]',
}
