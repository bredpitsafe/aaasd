import { style } from '../../../utils/css/style.css';

export const cnField = style({
    width: '100%',
});

export const cnTroubleshootingContainer = style({
    textAlign: 'right',
});

export const cnLabel = style({
    whiteSpace: 'nowrap',
    display: 'flex',
    alignItems: 'center',
});

export const cnButtonPosition = style({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'right',
});

export const cnTimeZoneSelector = style({
    width: '100%',
});

export const cnTimeZoneName = style({
    display: 'flex',
    flexFlow: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
});
