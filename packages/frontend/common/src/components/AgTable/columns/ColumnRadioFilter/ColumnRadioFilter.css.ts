import { style } from '../../../../utils/css/style.css';

export const cnContainer = style({
    padding: '8px',
});

export const cnSelect = style({
    height: '18px',
    margin: '3px 0',
    borderColor: '#dddddd',
    borderRadius: '4px',
});
