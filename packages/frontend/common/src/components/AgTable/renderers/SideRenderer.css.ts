import { green, red } from '../../../utils/colors';
import { style } from '../../../utils/css/style.css';

export const cnAsk = style({
    color: red[5],
});
export const cnBid = style({
    color: green[5],
});
