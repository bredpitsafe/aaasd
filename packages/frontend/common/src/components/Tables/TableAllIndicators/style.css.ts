import { style } from '../../../utils/css/style.css';

export const cnDatePickerTableLabel = style({
    padding: 0,
    width: '210px',
});
export const cnDatePicker = style({
    width: '100%',
});

export const cnRoot = style({
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%',
});

export const cnTable = style({
    flex: '1 1 auto',
    minHeight: 0,
    overflow: 'auto',
});
