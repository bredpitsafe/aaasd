import { style } from '../../../../utils/css/style.css.ts';

export const cnTooltipContainer = style({
    maxHeight: '300px',
    overflowY: 'auto',
});
export const cnTooltip = style({
    minWidth: 'min(90vw, 400px)',
});
