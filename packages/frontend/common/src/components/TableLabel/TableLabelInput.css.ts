import { style } from '../../utils/css/style.css';

export const cnLabel = style({
    display: 'flex',
    justifyContent: 'stretch',
    alignItems: 'stretch',
    padding: '0!important',
});
export const cnInput = style({
    fontSize: 'inherit',
});
export const cnFlexGrow = style({
    flex: '1 1 auto',
});
