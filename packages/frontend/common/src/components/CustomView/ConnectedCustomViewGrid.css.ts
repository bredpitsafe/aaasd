import { style } from '../../utils/css/style.css';

export const cnSpinner = style({
    textAlign: 'center',
});
export const cnGrid = style({
    display: 'grid',
    gridAutoRows: 'min-content',
    gridAutoFlow: 'column',
    gap: 0,
});
