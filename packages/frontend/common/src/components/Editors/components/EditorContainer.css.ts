import { style } from '../../../utils/css/style.css';

export const cnRoot = style({
    position: 'relative',
});
export const cnMonaco = style({
    position: 'absolute',
    zIndex: 0,
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
});
