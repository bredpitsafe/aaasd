import { style } from '../../utils/css/style.css';

export const cnList = style({
    overflow: 'auto',
    flexGrow: 1,
    width: '100%',
    padding: '8px',
});

export const cnAlert = style({
    width: '100%',
});
