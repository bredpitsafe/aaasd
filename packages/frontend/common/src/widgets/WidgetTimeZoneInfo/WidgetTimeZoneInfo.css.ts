import { style } from '../../utils/css/style.css';

export const cnTimeZone = style({
    textAlign: 'center',
});

export const cnTimeZoneIcon = style({
    width: '32px',
    height: '2em',
    padding: '2px 0',
    textAlign: 'center',
});
