import { style } from '@frontend/common/src/utils/css/style.css';

export const cnBadgesList = style({ display: 'flex' });
export const cnBadgesListVertical = style({
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '8px 4px',
});
