import { style } from '@frontend/common/src/utils/css/style.css';

export const cnContainer = style({
    width: '100%',
});

export const cnComponentName = style({
    width: '100%',
    whiteSpace: 'nowrap',
    textOverflow: 'ellipsis',
    overflow: 'hidden',
    textAlign: 'left',
    cursor: 'default',
    padding: '0 10px',
    borderRadius: 0,
});

export const cnSpace = style({
    width: '100%',
});

export const cnStageSwitch = style({
    width: '100%',
    minWidth: 0,
    overflowX: 'auto',
});
