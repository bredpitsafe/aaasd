import { style } from '@frontend/common/src/utils/css/style.css';

export const cnTableContainer = style({
    display: 'flex',
    flexDirection: 'column',
});

export const cnTableActionsFiller = style({
    flex: '1',
});
