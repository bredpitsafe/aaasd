import { style } from '@frontend/common/src/utils/css/style.css';

export const cnCheckIcon = style({
    color: '#0958d9',
});

export const cnHeaderDivider = style({
    margin: '4px 4px',
});

export const cnFooterDivider = style({
    margin: '8px 4px',
});

export const cnButtonsBlock = style({
    margin: '0px 12px',
    boxSizing: 'border-box',
});

export const cnApplyButton = style({
    width: 'calc(100% - 56px)',
});
