[service]
stage = "dev"

[oauth]
devUrl = 'http://127.0.0.1:8101'

[metrics]
prefix = 'dashboard_storage_'
labels = [["stage", "dev"], ["nodeNo", "0"]]

[subscriptions]
timeout = 5000

[authentication]
checkExpirationInterval = 1000
