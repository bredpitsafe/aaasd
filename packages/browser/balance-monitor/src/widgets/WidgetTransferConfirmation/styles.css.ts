import { style } from '@frontend/common/src/utils/css/style.css';

export const cnNetworkSelector = style({
    width: '100%',
});

export const cnActionsContainer = style({
    display: 'flex',
    flexFlow: 'row wrap',
    justifyContent: 'flex-end',
    gap: '8px',
    marginTop: '12px',
});

export const cnActionButton = style({
    flex: '0 0 150px',
});
