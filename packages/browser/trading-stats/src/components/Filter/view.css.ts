import { style } from '@frontend/common/src/utils/css/style.css';

export const cnForm = style({
    width: '100%',
    padding: '8px',
});
export const cnDivider = style({
    margin: '12px 0 4px',
});
export const cnSubmitButtons = style({
    margin: 0,
});
