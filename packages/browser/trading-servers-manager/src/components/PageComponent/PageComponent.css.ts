import { gold } from '@ant-design/colors';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnRoot = style({
    display: 'flex',
    height: '100%',
});

export const cnNav = style({
    display: 'flex',
    height: '100%',
});

export const cnProdHighlight = style({
    backgroundColor: gold[0],
});

export const cnLayout = style({
    position: 'relative',
    flex: '1 1 auto',
});
