import { green, red } from '@frontend/common/src/utils/colors';
import { style } from '@frontend/common/src/utils/css/style.css';

export const cnSide = style({
    fontWeight: 'bold',
    fontSize: 12,
});
export const cnBuy = style({
    color: green[5],
});
export const cnSell = style({
    color: red[5],
});
