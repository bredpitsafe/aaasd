import { ColumnsType, ColumnType, default as Table, TableProps } from 'antd/lib/table';

const { Column, ColumnGroup } = Table;

export { Table, Column, ColumnGroup };
export type { TableProps, ColumnType, ColumnsType };
