import { style } from '../../../utils/css/style.css';
import { styleTableRowHeight } from '../../AgTable/styles';

export const cnIncreasedRowHeight = style({});
styleTableRowHeight(cnIncreasedRowHeight, 40);
