import { style } from '../../utils/css/style.css';

export const cnNetwork = style({
    display: 'flex',
    alignItems: 'center',
});
export const cnNetworkName = style({
    marginLeft: '4px',
});
export const cnLog = style({
    display: 'flex',
    flexDirection: 'column',
});
export const cnLogItem = style({
    marginBottom: '8px',
});
export const cnLogTime = style({
    marginRight: '4px',
});
