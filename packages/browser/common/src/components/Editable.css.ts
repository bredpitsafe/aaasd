import { style } from '../utils/css/style.css';

export const cnRoot = style({
    position: 'relative',
    display: 'inline-flex',
    alignItems: 'center',
});
export const cnIcon = style({
    position: 'absolute',
    right: '0px',
    transform: 'translateX(100%)',
    padding: '4px',
    background: 'inherit',
});
export const cnWithCursor = style({
    cursor: 'pointer',
});
